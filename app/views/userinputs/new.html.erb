<%= form_for @userinput, :url => { :controller => 'userinputs', :action => 'create' } do |f| %>
    
    <%= f.collection_select(:vivocamera_id, Vivocamera.all, :id, :name) %>
    <%= f.label :property, "Property" %>
    <%= f.text_field :property %>

  
  
    <%= f.submit "Create" %>
<% end %>